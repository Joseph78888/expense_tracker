import 'package:flutter/material.dart';

class ExpenseInfoSceen extends StatelessWidget {
  const ExpenseInfoSceen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Expense Info Screen')));
  }
}
